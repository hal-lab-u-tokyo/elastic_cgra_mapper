# Archived 2D Placement Manifests

These are older exploratory settings kept for traceability. Do not use them as starting points for new comparisons.

Use `../search.json`, `../all_mappers.json`, or `../reproduction/` instead.
