# 2D Placement Probe Manifests

These manifests are for convergence checks, PRISA ablations, ILP spot checks, and implementation diagnostics. They are not default comparison settings.

For ordinary experiments, use `../search.json` or `../all_mappers.json`.
